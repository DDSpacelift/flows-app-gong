{
  "name": "flows-app-gong",
  "version": "1.0.0",
  "description": "Gong integration for Spacelift Flows - read-only access to call data, transcripts, and user information",
  "type": "module",
  "dependencies": {
    "@slflows/sdk": "^0.0.7",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "@useflows/flowctl": "^0.1.1",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "setup": "./setup.sh",
    "bundle": "npx flowctl version bundle -e main.ts",
    "format": "npx prettier --write .",
    "typecheck": "npx tsc --noEmit"
  }
}
